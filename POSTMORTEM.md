# 事故记录

这个文件记录开发过程中真实发生过的错误，按**错误类型**分组而不是按时间。
每一条都写清：现象、根因、修法、以及**现在靠什么防住它**——只写不防等于没记。

**但这个文件本身不是成绩。** 用户说得很直接：「我要的是稳定，不出故障……靠谱是种
基本能力。」一份交付里出现十次故障加十次漂亮的修复，仍然是不合格的交付。下面很多条
的共同根因不是技术难度，是**改完直接推、推上去才验**——而仓库里 140 多项守护检查
在 `ci.yml` 之前从来没在 CI 里跑过，只在我记得跑的时候跑。

所以现在有两道机械闸门，不依赖任何人的自觉：

    bash scripts/preflight.sh     推之前跑，一条命令跑完全部检查
    .github/workflows/ci.yml      每次 push 都跑，bot 的提交也一样过

它们检查的东西刻意包含了几项"测试全绿也看不出来"的：入库的符号链接不许指回仓库
内部（会让 Pages 打包无限递归）、构建必须幂等、仓库里的生成产物必须是最新的。

排在最前面的三类各自重复发生过三到四次，是最该警惕的。

---

## 一、失败却报成功（重复 3 次，其中一次造成数据丢失）

**最严重的一类。** 一个环节失败了，但上层看到的是绿灯，于是没人去看。

| 现象 | 根因 | 现在靠什么防住 |
|---|---|---|
| 云端 11 篇中文内容全部丢失 | 推送重试循环用 `git push && break`，循环结束后不检查是否成功，也没有 `exit 1`。日志最后一行是 `push retry 8`，步骤却报 success，runner 销毁后 git 历史里查不到痕迹 | 两个循环都检查 `ok` 变量，失败时 `::error::` + `exit 1`，并在日志里写明"本轮 N 篇会随 runner 销毁" |
| 12 次模型调用全挂，站上不增不减，看起来像"今天没内容" | `run.py` 正常退出 0 | 「每次模型调用都失败且零发布」退出码 5，日志明写"这不是今天没内容，是部署有问题" |
| 评审调用失败时放行，等于评审一坏闸门就不存在 | `review.passes(None)` 返回 True | 改为返回 False：不发布、记软失败、下一轮重试。有回归测试 `test_review_unavailable_does_not_pass` |

**通用规则**：任何"失败时放行"的默认值都要问一句——如果这一层永久坏掉，我多久才会发现？
答案是"不会"就说明方向错了。

例外是有意为之的：选题闸门失灵时**应该**放行（它只是省钱的预筛，放行让稿子走到
成稿评审那道真闸门去），这个区别写进了 `test_triage_unavailable_does_pass`。

| 日更 cron 整轮失败，站上不更新 | `run.py` 发布成功后会自己 build，留下未跟踪的 `p/` 与 `s/`；紧接着 workflow 的 `git pull --rebase` 要覆盖它们，报 `could not detach HEAD` 中止，8 次重试全败 | CI 调用加 `--no-build`（workflow 后面统一从合并后的数据重建），并在 rebase 前 `git clean -fdq p s`。检查见 `test_ci_does_not_build_before_rebasing` |

**这一条和第一条是同源的**：生成产物参与了合并。数据文件名唯一、永不冲突；
生成产物应该从合并后的数据重建，任何让它们进入 merge/rebase 的写法都会周期性
炸掉，而且炸在推送环节——正好是最容易被误报成 success 的地方。

---

## 一之二、让还在调的脚本改共享状态，然后汇报时没核对

新加的信源策展器我一边调一边跑，而它每轮都直接写 `data/sources.json`。两轮旧标准
的候选就这样进了清单：从 61 档变成 71 档，其中包括衍生源「硅谷101|中国版」——
它先进来之后，下一轮去重反而把**原版硅谷101**判成了重复。

更糟的是，我在汇报"新标准判别力更强"的时候，**根本没检查清单被改成了什么样**。
是下一轮的去重日志替我发现的。

修法：`curate.py` 加 `--dry-run`（只出建议，一个字不写）。定时任务保留自动执行，
但**自己调试时一律 dry-run**。已回滚到 61 档。

**通用规则**：一个还在改的判据，不该有权限直接修改会被后续步骤读取的共享状态。
汇报前跑一次 `git status` / `git diff --stat`，看清自己动了什么。

---

## 二、基础设施抖动被当成内容缺失（重复 5 次）

网络抖动、限流、配额用尽，被记成"这一集没有文稿"，消耗完重试预算后**永久拉黑**
一集本来合格的内容。

| 现象 | 根因 |
|---|---|
| 模型 529 Overloaded 让一集永久失败 | HTTP 后端有退避，CLI 后端没有，529 只表现为非零退出码 |
| CLI 多实例并发直接退出 1、无输出 | `--max-turns 1` 太紧，思考用掉那一个 turn 就被判超限 |
| 有字幕的硅谷101 E249 被自己的诊断跑批拉黑 | YouTube 429 限流和 CI 的机器人拦截都被记成 no-transcript |
| `deepseek-reasoner` 12/12 全挂 | 推理 token 计入 `max_tokens`，8000 被思考用光，`content` 返回空字符串，报错只说"没返回 JSON" |
| 策展的"feed 失效 → 移除"会误删好源 | Rational Reminder 的频道 ID 经搜索确认正确，但 Atom feed 返 404（同一天的体检还是 ok/15 条）—— YouTube 对密集请求限流。一次失败就删源，而**误删一个好源没人会注意到** |

第五次之后的修法不再是打补丁：`resolve_sources --check` 记 `fail_streak`，
成功归零、失败累加；策展的移除规则要求连续 3 次失败。
检查见 `test_source_removal_needs_a_persistent_failure`。

**现在的防法**：`transcript.last_was_transient()` 让取稿层告诉编排器失败的**性质**。
限流／要求登录／连接中断／ASR 网络失败算软失败（上限 8，不消耗重试预算）；
只有 yt-dlp 明确报 `has no automatic captions` 这类确定性结论才算硬失败（上限 3）。
推理模型自动抬 `max_tokens`，空 content 时报错直接指出是预算被思考吃光。

---

## 三、校验规则误杀真内容（重复 3 次，都在数字上）

闸门本身是对的，但判据太窄，把真的内容删掉了。**误杀比漏杀更难发现**，因为
日志上看起来是"闸门在工作"。

三次都是同一个盲区：**口语里的数字不是写出来的**。

| 成稿写的 | 逐字稿里实际是 | 第几次踩到 |
|---|---|---|
| 2014 年 | twenty fourteen | 第一次 |
| 约 400 美元 | four hundred dollars | 第一次 |
| 250 人/平方公里 | two hundred **and** fifty | 第二次 |
| 15 亿 | one point five billion | 第二次 |
| 125 亿 | twelve **and a half** billion | 第二次 |
| 2.2 亿 → 220 million | 浮点噪声 `2.2*1e8/1e6 = 220.00000000000003`，精确比较把整数误判成小数 | 第三次 |

**现在的防法**：`gate._renderings()` 把每个数字展开成所有可能的口语形态（英文单词、
年份读法「nineteen ninety one」、`and` 变体、分数惯用语、中文数字、跨单位换算），
并用容差比较浮点。这些真实逐字稿原句全部写进了 `tests/test_units.py::Numbers`。

同类误杀还有一次不在数字上：归属校验的中文分支用贪婪正则把标题切成整个短语
（「关于理想造芯」「这是你该知道的一切」），逐字稿里永远不会出现这种标题式说法，
于是一份**正确的 80 分钟本地转写被丢掉**。改成拉丁词 + 中文 3-gram 双通道。

---

## 四、按过时结论继续推理（重复 2 次）

环境变了，但我还在用旧结论说话，而且是**对用户说**。

- 转写打通之后，我仍然说"硅谷101、科技早知道只有 YouTube 字幕，云端 IP 被拦，
  必须本机跑"。实测六档中文源每集都带音频，ASR 在云端能跑，六档云端全都能做。
- 同一天我又说本机线"只对 5 档必要、优先级低"；用户把重心转到 YouTube 之后，
  这话立刻不成立——纯 YouTube 源只能住宅 IP 抓，本机线变成必需。

**防法只能靠自觉**：每次解开一个约束，回头重算依赖它的所有结论。写在这里是为了
下次改动能力边界时先做这一步。

---

## 五、字符串替换的锚点没匹配，静默失败（重复 2 次）

用脚本改代码时，锚点因为上一轮改动已经变了，`str.replace` 找不到就什么也不做，
**不报错**。

- 加分角色模型时改了 `_cli` 的调用处，签名的替换没匹配上 → `TypeError: _cli() got
  an unexpected keyword argument 'model'`。CI 走 API 路径所以一直正常，只在本机炸，
  10 篇补评全废。
- 首页 JSON-LD 的插入没匹配上，自查时才发现首页没有结构化数据。

**防法**：所有脚本化改动都用 `assert old in s` 断言锚点存在（现在是习惯），
改完立刻跑一次实际调用而不是只 import。回归测试
`test_cli_backend_accepts_a_model` 直接检查签名。

---

## 六、信源假设没验证就当结论

- 以为 Conversations with Tyler 和半拿铁"官网有文稿可走 page 层"，实测拿不到。
  两个都在加源前被挡下了，因为**加源流程强制实测文稿可得性**。
- iTunes 按名字匹配给出 9 个"有 RSS"的 YouTube 源，逐个比对集标题后只有 3 个是
  同一档节目。最危险的是 Anthropic 那条 `feeds.podcastai.com`——一个**冒用品牌的
  AI 生成新闻播客**。按名字匹配就加，站上会出现挂着 Anthropic 名头的机器编内容。

**防法**：加源必须实测取到文稿；换 feed 必须比对集标题与已知渠道，标题对不上一律
不用。`resolve_sources.py --check` 每周跑一次。

---

## 七、其他一次性的

- **SSL 只修了一半**：用 truststore 只给自己的 urllib 建了上下文，没有全局注入，
  于是 `huggingface_hub` 下模型时照样 `CERTIFICATE_VERIFY_FAILED`。修成
  `inject_into_ssl()` + 从 keychain 导出 PEM 并设 `SSL_CERT_FILE` /
  `REQUESTS_CA_BUNDLE` / `HF_HUB_CA_BUNDLE`。
- **把本地时间当成 UTC**，算出跑批"卡死 14 小时"（实际 21 分钟），**取消了一个健康
  的跑批**。现在报时间一律先 `date -u`。
- **直接跑 `run.py` 而不是 `local-daily.sh`**：前者不含提交推送，8 篇产出躺在本地
  没上线（本地 35 / 线上 27），用户问起才发现。
- **复制了被终端截断的 URL**（显示限 52 字符），拿去请求得到 404/403，一度以为是
  源坏了。
- **`.tag` 类名撞车**：masthead 的 slogan 和卡片上的标签小胶囊同名，slogan 因此套上
  了边框和底色。改名 `.slogan`，并写脚本扫全站同类冲突。
- **h1 数量**：单集页原来有两个（报头字标 + 文章标题）；改成字标只在首页当 h1 之后，
  信源页和 404 又变成零个。现在每页恰好一个，自查脚本会数。
- **`git add -A` 把源码扫进 bot 提交**：`local-daily.sh` 的提交把我未提交的
  `run.py` 改动打包进了 "build: regenerate site"。改成只 add 数据与生成产物的具体路径。

---

## 八、判据把「可核对」等同于「有价值」

用户反问「理财哲理漫谈为什么不好，我还挺喜欢哲学」。他是对的。

整个站的前提是"每条判断都能跳回原声核对"，于是我不自觉地把可核对等同于有价值：
数字多的加分，讲道理的减分。一集 Bill Bernstein 讲「哪些理财决定真正重要」被拒，
理由写着"无可核对数据或机制"。

真正的分界线不是有没有数字，是**能不能被反驳**：
  「要长期投资、控制情绪」                                   无法反驳的套话 → 拒
  「只有储蓄率、资产配置、不恐慌三件事重要，其余全是噪音」        有立场能反驳 → 收

triage / review / curate 三处判据都改了：明写价值有两种形态，形态二是判断与框架
（有立场能被反驳的论断、把复杂问题拆成少数起作用的变量、挑战流行共识并给出边界），
思想史与方法论只要得出可争辩的结论就算高分；低分那条改为"泛泛而谈的正确话"。

**一般化的教训**：把工程上的约束（我们能验证什么）写成了产品上的judgment
（什么是好内容）。这两件事必须分开——前者决定"能不能做"，后者决定"该不该做"。
同一个错误在策展评分里也犯过一次：文稿可得性占 3 分，于是一档有官方逐字稿的
平庸节目起步就有分，"能不能做"买到了"该不该做"的通行证。

---

## 八之二、让模型凭名气打分，然后过度纠正到把整条线关死

策展的信源评分，模型只看到「节目名 + 最新一集标题」。而实测环节明明已经把文稿抓下来
了，却只把**字数**传进提示词，正文扔了。

后果不是分数偏高或偏低，是**分数不稳**：同一档节目（AI实话实说）两次跑分都是 8.0，
一次理由里带对冲词被一致性检查拒掉、一次没带就收了。能不能进站取决于措辞运气。
一致性检查（HEDGE 短语表）也漏掉了"密度略逊于顶级"这种写法——短语匹配天生不全，
它只能当第二道，第一道必须是让模型看见真实内容。

把文稿抽样传进去之后判别力立刻变了：Waveform、This Week in Tech 从 6 分掉到 4-5 分
（"多为新闻转述"），理由开始引具体内容（"红杉IC构成、Momenta投资细节"）。

**但第一版修法立刻犯了第二个错**：只认文稿当证据，于是所有前三层取不到文稿的节目
一律被判"无文稿难核"扣到 4 分。而中文播客前三层本来就没有文稿——我们靠音频转写。
等于把整条中文信源线永久关死，而这是站点三大支柱之一。

这和第八条是同一类错误：又让**我们验证起来方不方便**决定了**该不该收**。修法是
三级证据梯子：文稿 → 分集说明（小宇宙／喜马拉雅常有两三千字，是关于主题与具体程度
的真证据）→ 确实没有；提示词里明写"没有文稿只说明我们要走音频转写，那是前置条件、
已经判过了，不进你的打分"。

**现在靠什么防住**：`test_score_prompt_carries_the_excerpt` 断言三级梯子都在，
`test_missing_transcript_is_not_a_scoring_penalty` 断言提示词里有"不进你的打分"，
`test_excerpt_samples_the_whole_episode` 断言抽样覆盖后半段（只看开头会被片头骗过去），
`test_hedge_catches_the_phrase_that_slipped_through` 把混过去的那句原话钉在测试里。

附带一条同源的设计错误：模型自报的 tier 被直接采信，六个新源全进了 tier 1，和跑了
半年的源同权抢配额——而它们的分是照着节目自己写的**分集说明（宣传文案）**打的，
一篇成稿都没过闸门。改成 `PROBATION_TIER = 3` 一律先试用，提级降级都交给体检。
`test_curate_ignores_the_model_tier_for_new_sources` 顺带断言不许改回去。

---

## 八之三、dry-run 的建议只打在屏幕上

上一轮 `--dry-run` 推荐了 3 档，我用 `tail` 看输出，名字被截掉了。要再看只能重跑，
而重跑要花 20 多次模型调用的钱。

**一般化的教训**：一次运行的产出如果只存在于滚屏里，等于没有产出。改成同时写
`.cache/curate-dry-run.json` 并在末尾逐条列出建议。`test_dry_run_persists_and_lists`
断言这两件事都在。

---

## 八之四、为了本地预览往仓库里放了一个指回自身的符号链接

为了让本地预览的路径和线上的 `/podcast/` 对上，我加了 `.preview/podcast -> ..`
并提交了。这是一个**指向仓库自身的符号链接**：GitHub Pages 打包时顺着它递归，
`Upload artifact` 连着两轮各卡死 20 分钟，`Checkout` 之前的步骤全绿。

表面症状是"代码推上去了但站上没变"——用户看到的就是还没发布。我先当成 GitHub
侧的卡顿，取消重跑了一次，第二轮卡在同一步才想到去比对：加它之前的那轮部署是
成功的。

**一般化的教训**：给本机方便加的东西，不该进会被流水线消费的目录树。判断标准很
简单——这个文件会被 checkout 到 CI 里吗？会的话，它就是生产配置的一部分。

**现在靠什么防住**：`test_no_tracked_symlink_resolves_inside_the_repo` 扫
`git ls-files -s` 里 mode 为 120000 的条目，解析目标，指回仓库内部就报错；
`.preview/` 进了 .gitignore。

顺带一条同源的：这个 launch.json 原来写的是 `--directory ..`，把仓库父目录挂到
本地服务上——那个目录里有含明文 token 的 `ghkit.py`。改掉了，但实测预览进程的
cwd 就是父目录、`--directory` 参数被忽略，所以真正的修法在文件本身（把 token
从 ghkit.py 里挪走），不在服务配置。

---

## 八之五、装完定时任务就当它在跑（从没跑过的定时任务等于没有）

launchd 的 plist 装好了、`launchctl list` 里也在，我就当本机线已经在工作。用户
问起"定时运行还在进行么"，我去查才发现 `runs = 0`、`last exit code = (never
exited)`——**装上之后一次都没跑过**。手动踢一下立刻看到原因：

```
shell-init: error retrieving current directory: getcwd: ... Operation not permitted
/bin/bash: /Users/…/Desktop/boke/podcast/scripts/local-daily.sh: Operation not permitted
```

仓库在 `~/Desktop` 下，而 Desktop／Documents／Downloads 受 macOS TCC 保护。
LaunchAgent 没有应用身份，读不了。权限位是 755、没有隔离属性，都排查过了。

它会在每天 21:30 静默失败，而"站上没更新"这件事没有任何人会注意到——和第一类
（失败却报成功）是同一个形状：**没有信号的失败**。

修法不是让用户去系统设置里给 /bin/bash 开完全磁盘访问（那既要 GUI 授权又是坏
实践），而是给定时任务一份自己的工作副本，放在 `~/Library/Application Support`
下（不受 TCC 保护），自己 clone、自己 pull、自己推——和云端 runner 一个路子。
用户日常那份留在桌面，原封不动。`scripts/install-agent.sh` 把这套固化下来。

**一般化的教训**：装好 ≠ 在跑。任何定时任务装完必须**手动触发一次**，看它在
调度器自己的环境里（窄 PATH、无 TTY、无 TCC 授权、钥匙串能否非交互访问）真的跑
通，再宣布它在工作。同一个道理适用于云端 cron：daily.yml 修好之后我也是先
workflow_dispatch 跑了一次才算数。

**现在靠什么防住**：`test_agent_working_copy_is_outside_tcc_protected_dirs`
断言 plist 模板里不出现 Desktop／Documents／Downloads；
`test_install_script_tells_you_to_run_it_once` 断言安装脚本里写着先手动踢一次。

---

## 八之六、bot 的提交把源码回退了（第 2 次让 bot 有权改源码）

本机跑批推送成功之后，我下一次 `git pull --rebase` 撞了一堆冲突。查提交内容发现
bot 的 `digest + build (local)` 干了这些事：

| 文件 | 变化 |
|---|---|
| `scripts/install-agent.sh` | −45 行，整个文件被删 |
| `POSTMORTEM.md` | −33 行，八之五那一节被删 |
| `tests/test_guards.py` | −26 行，launchd 那三条守护被删 |
| `scripts/com.ourword.podcast.plist` | 改回受 TCC 保护的桌面路径 |

它没有恶意，也不是 `git add -A`——`add` 已经按第七条的教训收窄成具体路径了。
根因在推送重试里：

```
git fetch origin main
git reset --soft origin/main     # ← 只移动 HEAD，索引还是旧基线的整棵树
git add data/... $SITE_FILES
git commit                       # ← 于是把这期间别人推上来的源码全部回退
```

`--soft` 保留索引。bot 的副本是在我那几个提交**之前** clone 的，索引里那棵树自然
不含它们，提交下去就是一次干净利落的 revert。而提交信息写的是 "digest + build"，
**在历史里看不出来**。

改 `--mixed` 只修了一半：索引对齐远端之后，远端有而本机磁盘没有的数据文件在 git
眼里是"被删除"，`git add data` 会把别人刚发的内容删掉。三步都必需：

```
git reset --mixed origin/main                                   # 索引对齐远端
git diff --name-only --diff-filter=D -- data | xargs -n1 git checkout --   # 取回缺失的数据
python pipeline/run.py --reconcile                              # state 从磁盘重建
```
（`--reconcile` 是为此新加的：state.json 是索引不是真相，永远从 data/episodes 重建，
不参与三方合并。）

**一般化的教训**：自动化提交的危险不在于它写了什么，在于它**替谁写**。凡是 bot
能提交的路径，都要问一句"它会不会把我刚写的东西覆盖掉"——而且 bot 的提交信息
天然不会告诉你。第七条修的是"别把源码 add 进来"，这次是"别让索引替你回退源码"，
同一个权限边界的两个漏口。

**现在靠什么防住**：`test_no_soft_reset_in_any_push_path` 扫四处推送路径不许出现
`reset --soft`；`test_data_retry_restores_files_deleted_from_the_worktree` 断言
取回删除 + reconcile 都在；`test_reconcile_flag_exists_and_needs_no_llm` 断言
`--reconcile` 的早退在其余初始化之前（重试路径不该因为没配 key 而失败）。

附带一条判据自身的教训：给重试循环加了个嵌套 `while` 之后，`test_every_push_retry
_loop_checks_its_result` 里配 `do…done` 的非贪婪正则提前收口，含 `git push` 的
循环被整段跳过——**检查静默失效**。判据改成从 `for` 往后取固定窗口，并加
`test_the_guard_survives_a_nested_loop` 把这个坑钉住。又一次印证：判据要编码
失败机制，不要照抄当时那次失败的文本形状。

---

## 八之七、真正的根因是「没有信号」，而我一直靠被问起来当告警

用户说：「你要把系统搞稳定，别每次我问你你才去查去补。」这句话点出的是前面
所有条目的公共部分。回头看这些故障：

| 故障 | 谁发现的 |
|---|---|
| Pages 部署卡在 Upload artifact 21 分钟 | 用户问「还没发布」 |
| 本机定时任务装好后 `runs = 0`，一次没跑 | 用户问「定时运行还在么」 |
| bot 的提交把源码回退了 | 我下一次 pull 撞冲突，偶然 |
| 日更 cron 整轮失败 | 用户问起时我才去翻 |
| 推送重试 8 次撞同一个未合并冲突 | 同上 |

没有一个是系统自己报出来的。每一条我都单独修了，但**"靠人来问"这件事本身**
一直没修——等于把用户当成了监控系统。

修法是三件事，缺一不可：
1. **心跳**：两条线各写 `data/heartbeat-{cloud,local}.json`。没有心跳，
   "这条线从没跑过"根本无法和"这条线今天没内容"区分开。空轮也要写，
   否则"闸门全拦下"和"根本没跑"分不开，而这两件事的处理完全不同。
2. **体检**：`pipeline/healthcheck.py` 把可观测的不变量列成判据——心跳超时、
   内容停更、数据／正文页／短链三个数字不相等、**线上篇数和仓库不一致**
   （这条专门抓"推了但没部署"）、关键页取不到、cron 被 GitHub 静默停用。
3. **告警**：`watch.yml` 一天三次，坏了开 issue（邮件到人），复用同一个 issue
   追加而不是每次新开（否则告警自己变成噪音），自愈后自动关闭（否则下次真坏了
   没人相信它）。

**而告警本身也必须被验证过。** 加了 `-f selftest=true` 演一次假故障，实测：
issue #1 自动开出、下一轮体检通过后自动关闭。没试过的告警等于没有告警——
这正是 launchd 那次的形状（装好了，从没跑过）。

**一般化的教训**：修一个故障和修"这类故障不会被发现"是两件不同的工作，后者
才是稳定性。判断标准很简单——如果这件事再坏一次，我需要用户告诉我吗？

---

## 八之八、新加的监控自己静默失效了（同一天）

心跳上线当天就没写出来。第一版是 shell heredoc，嵌在一个被管道接走的花括号块里：
单独执行那段完全正常，真跑批时一声不响地什么都没产生。**心跳的全部意义就是
"没跑会被发现"，它自己静默失效等于白做。** 换成 `pipeline/heartbeat.py`，
并且让它把"心跳已写 …"打出来。

同一次还抓到本机脚本的 `SITE_FILES` 清单漏了 `e`（分享短链）和 `log`（更新
日志）——本机跑批**永远不提交这两样**，日志里躺着一堆未跟踪的 `e/` 目录。
云端用 `git add -A` 所以完全看不出来，只有本机线在悄悄少推东西。抓到它的正是
新加的那条"数据／正文页／短链三个数字必须相等"。

**教训**：新加的监控要当成被监控对象来验，不能当成裁判就默认它是对的。
清单式配置（`SITE_FILES` 这种）每加一样新产物都必须跟着改，而它漏掉的时候
不会报错——所以要有一条不变量去对它。

---

## 八之九、在好判据前面加了个手写正则，静默丢掉一半池子

用户给了 4918 档播客让我筛，我报"零档过关"。他的反应是「这么多内容源一个也没收录
么，怎么可能」——他对了，问题在我的漏斗。

我的第一步是手写关键词正则：名字里得出现 ai / invest / history / science 这类题材词
才留下，4918 → 396。而**播客名字常常根本不含题材词**。拿我们自己在册的源去试那个
过滤器：

    149 档在册源里，74 档会被直接丢掉

包括 Dwarkesh Podcast、Acquired、Odd Lots、EconTalk、Conversations with Tyler、
In Our Time、Lex Fridman Podcast、Freakonomics Radio、Hard Fork、No Priors。
**一半的池子在第一步就没了，而且是静默的**——我只看得见活下来的那部分，
"零档过关"看起来像池子质量差，其实是漏斗漏了。

仓库里本来就有对的工具：`shortlist()`，用站点判据批量粗筛名字。我却在它前面加了
一层自己拍脑袋的正则，理由是"先省点调用"。省下的是几十次便宜模型调用，代价是
把一半候选扔了。

**一般化的教训**：在一个已经过验证的判据前面加任何"预过滤"，都要先问它会不会
误杀。**手写规则的误杀是不可见的**——你看不到被丢掉的东西。而这件事有个现成的
检验：拿**我们自己已经接受的东西**去跑这个过滤器，它要是把它们拒了，过滤器就是
错的。这个检验对任何选择性判据都成立，不限于信源。

**现在靠什么防住**：`test_no_prefilter_would_reject_our_own_sources` 把这条钉住——
任何在 curate 里做名字级预筛的正则，都要拿 sources.json 里在册的源验一遍。

---

## 九、评分锚定在及格线（重复 2 次）

第一次在成稿评审：已上站的 16 篇分数**全部正好 8**。第二次在信源策展：一批候选
**7 档全部正好 8.0**，而理由栏里写着"补位有限""部分集数偏泛"。

两次都是同一个成因：判据把及格线写成了"合格"、把更高分写成了"罕见"，于是模型把
所有正常候选挤到及格线上。**全部拦稿权力压在 1 分的间隙上，对噪声极度敏感。**

修法两条，缺一不可：
1. 提示词里明说"如果你发现自己给一批候选打的分都一样，那不是在评分，是在往及格线
   上凑"，并把区间每一档写清。
2. **不要指望模型自觉，用代码判一致性**：理由里含"重叠／偏泛／有限／不稳"这类词
   而分数刚够线的，一律不收。实测拦下 4 档自相矛盾的 8.0，留下 Darknet Diaries
   10.0（密度5/补位3/可核对2）。

---

## 十、同一个函数改四次才对

`_name_match`（判断搜到的节目是不是想找的那一档）：
1. 10 字符前缀 → 搜「Patrick」匹配到了别的节目
2. 按最短一方的比例算命中数 → 把 show 当停用词过滤后对方只剩一个 token，
   「Tomorrow Today」匹配上了「The Tomorrow Show」
3. Jaccard ≥ 0.5 → 「This Week in Tech」和「This Week in Startups」判成同一档，
   靠 this/week 撑起相似度
4. Jaccard ≥ 0.5 **且交集里必须有一个有辨识度的词** → 6 个用例全对

`slug_for`（信源 id，出现在 URL 里）：把非 ASCII 全删，于是「42章经」塌成 `42`、
「脑放电波」塌成 `src`、「AI实话实说」塌成 `ai`。改为纯中文名用 `cn` + 名字哈希。

**教训**：字符串相似度这类函数，每改一版都要把**前几版失败的用例一起留在测试里**，
否则修 A 会打破 B。现在这些用例都在 `tests/test_units.py` 与 `test_guards.py` 里。

---

## 评审自身的两个设计缺陷

值得单独记，因为它导致我连续两轮判错方向。

通过率只有 12-21%，14/24 篇卡在 7 分（及格线 8）。我先怀疑生成、改了提示词，
结果更差。真因在评审：

1. **「选择力」这条标准在结构上无法满足**。两万五千字压成 5-8 条要点必然丢掉绝大
   部分，评审只要想挑永远挑得出遗漏，于是成了一项恒定扣分。
2. **更糟：评审在评判它没见过的东西**。给它的证据只有"每条引用时间戳前后 45 秒"，
   全都是成稿已经引用过的区域。它看不到未引用段落，却反复以"漏掉了更值钱的内容"
   扣分。
3. 打分基准把 8 写成"合格"、9-10 写成"值得转给同行"，读起来像 8 才是优秀线，于是
   所有正常稿子被挤进 7-8 两档——**已上站的 16 篇分数全部正好 8**。那不是评分，
   是二元判断，而全部拦稿权力压在 1 分的间隙上，对噪声极度敏感。

修法：明写"遗漏本身不是缺点，说不出具体的 B 就按满分算"；补第二份证据（全篇等距
抽样 4000 字）并声明抽样之外不构成遗漏指控；把区间展开并明说"如果你发现自己给几乎
所有稿子都打 7 或 8，那是你在做二元判断，等于把这套标准废掉了"。

**一般化的教训**：给模型的判据必须是它**能用手上的材料做出的判断**。判据要求它知道
它看不到的东西时，它不会说"我不知道"，而会编一个理由扣分。

## 十一、频道 id 指向剪辑号，兜底路径前面挡着两条提前 return

用户问"为啥看不了视频只有音频"。查下来四层，每一层都是我造的，而且我在查的过程中
又错判了一次。

**第一层：兜底路径前面有两条提前 `return None`。**

```python
chan = src.get("yt")
if not chan:
    return None            # ← 122 档信源从来没搜过
try:
    vids = feeds.fetch(...)
except Exception:
    return None            # ← 一次失败 = 这一集永远没有视频
```

频道 Atom feed 只是**快路径**，拿不到本该往下走 yt-dlp 搜索。这两条 return 把兜底
彻底切断，全站只有 55/255 有视频，而用户看到的现象只是"只有音频"。修掉之后，215 篇
只有音频的里配上了 57 篇，偏差 0～30 秒。

**第二层：那条快路径的端点已经废了。** `feeds/videos.xml?channel_id=…` 现在对
**任何** id 都返回 404——包括拿 yt-dlp 反查出来的、确认存在的真实频道
（Dwarkesh 的 `UCXl4i9dYBrFOabk0xGmbkRA`）。所以它不是"偶尔失败"，是一次都不会成功，
每集白花一次请求，日志里那行 `channel lookup failed: HTTPError` 看着像网络抖动。
已删除。

**第三层：存着的频道 id 有几个指向错的频道。** 43 个 id 逐个拉频道页验证，
**都存在**，但：

| sources.json 里写的 | 实际是哪个频道 |
|---|---|
| Dwarkesh Podcast | **Dwarkesh Clips**（剪辑号） |
| The Cognitive Revolution | Upstream with Erik Torenberg |
| Machine Learning Street Talk | Yannic Kilcher |
| The Twenty Minute VC | Business of Sport |

指向剪辑号正是片花的来源。**已经挂着视频的 55 篇里 16 篇挂的是错视频**：
80,000 Hours 挂了 176 秒的片花（音频 2968 秒）、Lenny's 挂了 133 秒（音频 5630 秒）、
Acquired 挂了 1674 秒对 14360 秒的正片。Atom feed 不带时长，只能比标题，而片花的
标题和正片几乎一样。这些页面上**每一个**时间戳都跳错——比没有视频糟得多。

**第四层：一个错字让一整档节目归零。** "张小珺"被拼成"张小珲"，搜索查询是
"节目名 + 集标题"，于是这档节目搜索恒定返回空——和"这集真没视频"在日志里毫无区别。
搜索改成两轮：先"节目名 + 标题"，再"只用标题"。

### 我在修的过程中又犯了同一类错

写完核对工具跑第一版，输出"43 档全部清空，一个都没有视频证据"。**我差点就这么写回去了。**
两个原因叠在一起：

1. 我拿那个**已经废掉的 RSS 端点**当"这个 id 存不存在"的判据，于是把"端点没了"
   读成"43 个 id 全是编的"——还照这个结论往 POSTMORTEM 里写了一节。换成拉频道页
   才发现 43 个全都存在，问题是**指向错了**，完全不同的结论和修法。
2. 反查真实频道用的 `yt-dlp`，批量跑撞上了 `Sign in to confirm you're not a bot`，
   **每一条都失败**。工具把"被临时封锁"读成"这档节目没有任何视频证据"，
   而它的处置是清空——**临时故障造成永久损失**。

`video_aligned` 因此从两态改成三态——True 验过一致、False 验过不一致、
**None 这次查不出来**，第三种一律保持原样，`--audit` 里过三成查不出来就报错退出。

至于那个字段本身：删掉快路径之后**没有任何代码再读 `src["yt"]`**。所以最后的处置
不是"修好这 43 个 id"，而是把字段整个删掉——留一个没人读、又验证不了的外部标识符
在配置里，唯一的用处是将来某天悄悄拿它抓错东西。要重新引入频道发现，必须连着
验证工具一起来；守护测试现在直接禁止清单里出现 `yt="UC…"`。

**一般化的教训**：
1. **兜底路径前面不能有提前 return。** 快路径失败该降级，不该终止。这是第二回了
   ——上次是"手写正则挡在好判据前面，静默丢掉一半池子"（第八之九节）。
2. **破坏性动作不能建立在"这次没查到"上。** 凡是删除、清空、摘掉，都必须能区分
   "验过了，不对"和"这次没验出来"。前者才动手。
3. **拿一个判据去下结论之前，先验证这个判据本身还成立。** 我用一个已经废掉的端点
   判定 43 个 id 的真假，得出的结论完全反了。
4. 凭印象填进配置的外部标识符等于没填，而且会伪装成偶发失败。
5. **改完判据要让"还没跑过"这件事自己冒出来。** 时长核对只在跑过之后才写下
   `video_len`，而守护测试是靠 `video_len` 离线复查全站的——不跑，那条守护就是
   空转，而空转的守护比没有守护更糟：它让人以为查过了。所以体检现在报"多少篇
   还没核对过时间轴"，日更每天自动核一小批（没核过的排前面），几天清完存量。

## 十二、样式表没版本号，读者拿旧 CSS 配新 HTML

用户连着三轮说"播放界面能再丑一点么"、"别跟半成品一样交给我"。我三轮都在改样式，
每轮都在浏览器里验过、每轮都是对的。**样式早就对了，读者根本没拿到。**

`site.css` 和 `site.js` 的引用是这样的：

```html
<link rel="stylesheet" href="/podcast/assets/site.css">
<script src="/podcast/assets/site.js" defer></script>
```

URL 从不变，而 GitHub Pages 给的是 `cache-control: max-age=600`。HTML 也是
600 秒——两者各自到期，于是有一个窗口：**新 HTML 配缓存里的旧 CSS/旧 JS**。

新 HTML 里有 `.frame` / `.vdur` / `.aui` 这些新结构，旧 CSS 里没有对应规则，
后果是一整套看起来像"没做完"的症状：

| 现象 | 实际原因 |
|---|---|
| 播放器卡片没描边、没圆角 | 旧 CSS 没有 `.player` 的卡片样式 |
| 封面不是 16:9，露黑边 | 旧 CSS 没有 `.frame{padding-top:56.25%}` |
| 时长掉到图片下面变成一行居中文字 | 旧 CSS 没有 `.vdur` 的绝对定位 |
| 播放圈完全看不见 | 它是个**空 span**，全靠 CSS 画 |
| 音频还是原生的灰药丸 | 旧 JS 不会去摘 `controls`、不会显自定义层 |
| "这篇是怎么来的"贴着分割线 | 间距修复在新 CSS 里 |

而"点播放器只有声音没有视频"也是这个：播放圈看不见，页面上唯一能点的就剩下面
那条原生音频。

修法两层：

1. **URL 带内容指纹**：`site.css?v=<sha256 前 10 位>`。指纹是内容的函数，
   所以构建仍然幂等（不然"构建是幂等的"那道闸门会红）；内容变了 URL 就变，
   浏览器必然重新取。调 `max-age` 只能缩短窗口，消不掉。
2. **脚本先确认样式表生效**：`site.css` 里定义 `--css:ok`，脚本读不到就不做
   依赖样式的 DOM 替换。把能用的原生控件换成一条没样式、看不见的进度轨，
   比不换糟得多。顺带把播放圈从"空 span 靠 CSS 画"改成内联 SVG——零 CSS 也在。

**一般化的教训**：
1. **同一个页面的两半可以来自不同版本，而这不是假设，是默认。** 只要 HTML 和它
   依赖的资源各自独立缓存，就一定存在版本错配的窗口。带指纹是唯一能消掉它的办法。
2. **我在本地验过"它是对的"，验不出"读者拿到的是对的"。** 这一轮我每次都在浏览器
   里量了盒子尺寸、截了图，全对——因为本地服务器没有那 600 秒。判据得贴着读者的
   环境，不是我的。
3. **渐进增强要指向"更少功能"，不能指向"更少可用性"。** 摘掉原生控件换自绘控件，
   在样式缺失时是净损失。做替换之前先确认替换后的东西真的能用。

## 十三、判据只给了标题，正文全靠模型自由发挥

用户："我觉得我们文章内展示的重点和金句还不够核心，再思考下内容如何更吸引人
更能突出视频播客本身要讲的核心"。

第一反应是展示问题（排版、字号、层级）。**不是。** 查 `digest.SYSTEM` 才发现：
标题有一整套硬判据（长度、只写一个论断、不许有需要背景才懂的词、五问自检，
带正反例），而 **dek、要点、金句一条判据都没有**——只有机械规则（必须逐字、
必须带时间戳、不许把推测写成结论）。也就是说"什么算核心"这件事从没定义过。

可量化的后果，全站 2095 条要点小标题里：

| | |
|---|---|
| 带否定/转折/断言标记的 | **331 条（16%）** |
| 名词短语式的话题名 | 约 84% |

随便抽几条就看得出来：「折旧与晶圆厂滞留风险」「与长鑫合作意义」「第一笔钱投向
哪里」「2017年的转折」「格局：两三家创业公司加一两个大厂」——这是节目的目录，
不是判断。读者扫一遍，拿不到任何他自己听一遍拿不到的东西。

**一个反面教训：我最初的诊断过度拟合到一篇。** 我照曾鸣那篇（小标题只有 4-6 字）
下结论说"小标题太短"，一量全站中位是 11 字、8 字以下只占 9%——短不是问题，
**是不是论断**才是。换成"带不带断言标记"这个量法之后，16% 这个数字才说明了问题。
判据要贴着失败机制，不是贴着我手上那个例子。

补上的三节判据：dek 只承载一个论断（原来能把四个论断串成一句）；要点先解决
"选什么"（会改变判断／给出机制／带着记得住的数字或比喻，三条都不满足就删掉），
小标题必须是一句能被反对的话，正文先给论断再给支撑，**留住说话人给的那个具体
画面**（服务器半夜崩了、快掉下桌子的模型火车、满嘴胡说的大学生）和他自己造的词
（formidable、jagged frontier）；金句是"这段论证的承重句"，不是格言。

效果（同一批稿子，只重出小标题）：24 条里带断言标记的 **5 → 17**。
「财务不当行为的定义」→「不当行为的判准是「这不是你的钱」」、
「CFP 认证的价值」→「只谈投资的顾问，做的不是财务规划」。

### 顺带两件事

**评审报的"最该改"是错的。** 用新判据发 Paul Graham 那集时，评审点名一处忠实度
错误，说我们把"多投几家、任一成功即可"的投资逻辑写成了治疗策略。我回原文查了：
PG 原话就是 `inflicting on cancer the death of a thousand cuts, which may be the
way to defeat it`——千刀万剐是他自己说的。**照评审说的"修"反而会改出一个真错误。**
不过顺着查出两处真的小毛病：他的对冲语气（may be / maybe）被写成了断言，
"该多投几家，任何一家成了就值"那句丢了。两处都补了。

**我自己差点污染全站时间戳。** 写回填工具 `repoint.py` 时，我在写盘前调了
`digest.normalize()`。normalize 是给**模型原始输出**用的，它把每个字段 str()
一遍，于是 `t: None` 变成 `""`——而 `hhmmss("")` 当时会抛 ValueError，
**把整站 267 篇的构建一起炸掉**。我是先手工对一篇调了 normalize、构建立刻挂了
才发现的，那时后台回填刚起来、第一篇还没写盘。`retitle.py` 和 `repunct.py`
一直是逐字段过 `_cn_punct`，正是因为这个。

**一般化的教训**：
1. **有硬判据的部分会变好，没判据的部分不会。** 标题的判据是被用户逼出来的，
   于是标题变好了；同一篇文章的正文没人给判据，就一直停在"把目录翻译一遍"。
   哪里没写判据，哪里就是下一个被投诉的地方。
2. **诊断不要停在手上那一个例子。** 先去全站量一遍，量出来的失败机制才配写成判据。
3. **回填工具只许改它声明要改的那个字段。** 对已发布的数据整体 normalize
   等于把模型输出那一层的假设套到成品上。这条已写成守护，四个工具一起管。
4. **一个字段的坏值不该让全站发不出去。** 判据该放在 normalize 那层拦，
   但 `hhmmss` 这种被几千处调用的格式化助手不能放大故障——现在它遇到认不出的值
   返回空串。

## 十四、机械替换的三处遮蔽，和一次未提交工作被卷走

做英文版时把 `main()` 的输出体抽成 `render_site(out, lang)`，好让同一套模板渲两次。
抽的办法是把函数体里的 `ROOT /` 全替换成 `out /`——**省下的时间全赔在这上面了。**

`out` 是函数参数，而函数体里有三个循环用 `out` 当循环变量：

| 位置 | 后果 | 症状 |
|---|---|---|
| 源站页 `out = sdir / src["id"]` | 之后的 `p/`、`e/`、`robots.txt`、`llms.txt` 全写进 `s/<最后一个源>/` | 仓库里多出一个 `s/tokcast/p/` |
| 短链 `out = edir / x["id"]` | 循环后 `out` 指向最后一集的目录 | **少了两个短链页** |
| 正文页 `out = pdir / x["slug"]` | 同上 | 同上 |

三处都不报错，症状离原因都很远（"少了两个短链页"、"多了个 s/tokcast/p 目录"）。
守护现在直接编在机制上：**`render_site` 函数体内不许给 `out` 赋值**，一条正则扫完。

同一次替换还漏了 `write_card_pages`——它定义在 `render_site` 之外、直接写 `ROOT`，
于是英文那趟把**根目录**的 `cards-*.json` 覆盖成带 `/podcast/en/` 链接的版本，
简体首页滚到第二页会全跳去英文站。这个是靠"简体输出必须逐字节一致"抓到的：
我在重构前后各取一次指纹做对比，不是靠眼看。

### 两个"本地能过、CI 会错"的

**`_has_en()` 读构建产物。** hreflang 要逐页判断"这一页有没有英文版"，我读的是
`en/p/<slug>` 目录。而简体那趟**先跑**——CI 上是全新 checkout，那时 `en/` 还不
存在，于是没有一个中文页会声明英文版。本地一直是对的，只因为上一轮的 `en/`
还留在盘上：**顺序依赖被残留状态掩盖**。改成读译文数据（两趟之前就都在）。

**环境变量控制输出。** 英文站原来由 `PODCAST_EN` / `PODCAST_EN_LIVE` 开关控制，
后果是**同样的数据能产出两种 HTML**：裸跑 `build.py` 的中文页不声明英文版，
工作流里带开关跑的声明——committed HTML 会在两次构建之间来回翻。而"构建是幂等的"
那道闸门只在**同一次调用**里比两遍，抓不到这种跨调用的分叉。现在由
`data/en/` 里有没有译文决定，开关全删，守护禁止再出现。

### 未提交的工作被另一个会话卷走

我在这个目录里改了几十处、没提交。同伴会话（另一个 Claude）在同一个工作树里
提交了一次 `build: regenerate site`，把我未提交的改动一起提交了；随后一次
`git pull --rebase` 把那个提交丢掉，我的改动跟着消失。

**找回来了**：reflog 里那个悬空提交（`ca962071`）的树里有我的工作，
`git diff <同伴的提交> <悬空提交> -- pipeline/ data/en/` 正好是我那一份增量，
`git apply --3way` 干净落地。

**一般化的教训**：
1. **机械替换省下的时间会加倍赔回去。** 把一个标识符批量换成另一个之前，先看
   目标名字在作用域里有没有被重新绑定。这次三处遮蔽 + 一处漏改，全是同一次替换。
2. **重构的验收判据是"输出逐字节不变"，不是"测试还绿"。** 我在重构前后各取一次
   全站产物指纹，两次不一致就去查——第一次就是这么抓到 `cards-*.json` 被污染的。
   而取指纹时要先确认输入没变（后台回填在改 `data/`，那会把"输入变了"报成
   "重构改坏了"，我为此在 preflight 里加了输入指纹）。
3. **"本地能过"要追问一句"CI 上第一次跑呢"。** 残留的构建产物会掩盖顺序依赖。
   判据要落在**两趟之前就存在的输入**上，不落在中途产物上。
4. **同一个工作树里有别的会话在动时，未提交的工作就是暴露的。** 频繁提交，
   哪怕是"半成品但自洽"的提交。这次能找回全靠 reflog 的悬空提交，那是运气。

## 25. 走查一遍站点，抓到 8 个自动检查看不见的问题

他说"站在用户的角度走查整个网站还有没有明显问题"。走查的价值在于**判据来自
读者会遇到什么**，而不是来自代码结构——所以它抓到的东西和守护测试抓到的
完全不重叠。这一轮 8 个问题，没有一个是现有 300 多项检查能报出来的。

### 三个 SEO/GEO 的洞：产物存在，但没人找得到

1. **`robots.txt` 只声明了简体的 sitemap。** robots.txt 只在**站点根目录**
   被读取，所以 `/en/robots.txt` 是死文件（我还生成了一份）。结果 `/en/` 和
   `/tw/` 只能靠中文页上的 hreflang 被发现——英文站等于半个隐身。
2. **`og:locale` / `inLanguage` / `<language>` 在 build.py 里硬编码 `zh_CN`
   七处。** 繁体靠 tw.py 做字符串替换救回来了，**英文站没人救**：每个英文页
   都在告诉社交平台"这是中文页"，英文 RSS 自称 `<language>zh-CN</language>`。
   教训：这类标记要有单一出处（`og_locale()` / `in_language()`），
   靠下游替换救等于每加一棵树都要重救一次。
3. **答案引擎读的那两份文件全是中文。** `en/llms.txt` 有 427 行中文、
   `en/llms-full.txt` 有 3717 行：一整套硬编码的双语标签（`发布 / published`、
   `核心论点 / points`）、163 条中文节目简介、中文分类名，还自称
   "written in Chinese"。**HTML 页面是零漏译的**——因为那道闸只扫 HTML。
   根因是这两个生成函数绕过了页面用的那套本地化 helper（`show_name` /
   `src_desc` / `T`），直接读中文字段。

### 五个读者一眼能看见的问题

4. **英文页的浏览器标签没有图标。** favicon 用了 `{BASE}/icon.svg`，英文页
   于是指向 `/podcast/en/icon.svg`——线上 404。图标只有一份，在仓库根，
   必须钉在 `BASE_ZH`（和 `asset()` 一个道理，但它没走 `asset()`）。
5. **5 篇已发布的集，点节目名是 404。** `addvideo.py` 单集入库时 `source_id`
   不进 `sources.json`，于是 `every`、`peteryang` 的信源页从来没生成过。
   修法按后果写：**任何有集的信源都必须有页面**，名字取自集本身。
   它只补页面，不改"每天抓哪些节目"——那是产品判断，不该由一处断链顺手决定。
6. **浅色主题下 76 处文字不到 WCAG AA。** 全是 11-13px 的日期、篇数、角标，
   用的都是 `--faint`。它此前从 2.69 修到 3.97，而 4.5 才是正文的线——
   "差一点"在 11px 上就是看不清。深色那边同一个变量更糟（3.70-4.25）。
   原来那条渲染检查的判据是 **3:1**，用意只是抓"某个颜色没跟着主题换"
   （白底白字）；那个目的它达到了，但它放过了真正难读的一档。
7. **site.js 里 10 处中文文案，英文读者全看得见。** 加载提示、全文索引提示、
   播放/暂停的 aria-label、"去 YouTube 打开原视频"、三条分享提示。
   **"零漏译"那道闸只扫静态 HTML，扫不到 JS 在运行时写进 DOM 的字。**
   英文读者点一下分享就弹一句中文。
8. **说话人字段 451 处是中文。** `translate.py` 从来没译过 `spk`——2143 条
   译文论点里带 `spk` 的是 0 条。其中「西格尔·塞缪尔」是 Sigal Samuel
   音译成中文、又原样端给了英文读者。**HTML 的零漏译闸门放行了它们**，
   因为渲染处包了 `lang="zh"`——那条判据的用意是保住人名的原文
   （中文节目叫「硅谷101」就该显示「硅谷101」），不是给漏译开后门。
   修法是单独一张全站去重的译名表（`transspeakers.py`，65 个值），
   按集译会把同一个人译出好几种写法。

### 两个走查顺手翻出来的

9. **`tw.py` 把点目录整个抄进了繁体树。** 它只挡 `SKIP_DIRS`，于是
   `.claude/` 和 `.cache/` 都进去了——`.cache/tr/` 里是第三方文稿，版权原因
   绝不能进公开仓库，而它们是 `.json`，正好在拷贝白名单里。唯一拦住它们的是
   `.gitignore` 里的一行（线上确认 404，没真流出去）。**一层网不是判据**：
   繁体树是**已发布站点**的字形转换，不是工作目录的镜像。
10. **本机预览服务把带明文 PAT 的文件服务了出去。** `boke/.claude/launch.json`
    里的 `python3 -m http.server 8899` 没有 `--directory`，服务根就是
    CWD=`~/Desktop/boke`，而那里的 `ghkit.py` 文档字符串里有明文 GitHub PAT。
    实测 `GET /ghkit.py` 返回 200。只在 localhost，但这条线是我自己铺的。
    预览服务的根一律指向 scratchpad 里的符号链接农场，那下面只有一个链接。

### 这轮真正的教训

**"检查全过"和"读者没问题"是两件事，而且差得很远。** 这一轮 8 个问题里：
- 3 个是**判据的作用域不够**（零漏译只扫 HTML，扫不到 .txt 和 JS 注入的字；
  对比度只查 3:1，够不上正文的线）。判据本身没错，覆盖面错了。
- 2 个是**同一个值有多个出处**（locale 标记七处硬编码、favicon 没走
  `asset()`）。下游补救能救一棵树，救不了第 N 棵。
- 2 个是**产物存在但入口缺失**（sitemap 没声明、信源页没生成）。
  产物齐全的检查全过，因为它们查的是"有没有生成"，不是"找不找得到"。
- 1 个是**工具在镜像工作目录而不是站点**。

还有一件事值得单独记：**走查过程中我自己的测量脚本错了三次**——
`color(srgb 0.95 …)` 的分量按 0-255 读、`position:fixed` 的元素用
`offsetParent` 判可见、改完 `data-theme` 同一回合就读 `getComputedStyle`。
三次都量出了不存在的 bug（3.05:1 的分享按钮、12 处繁体 chip、"分享没反馈"）。
**用一个没验过的尺子去量，比不量更糟**——它会把工期花在假问题上，
还会让真问题显得已经查过了。所以每一条量出来的结论，都要能反向注入验一遍。
