# 事故记录

这个文件记录开发过程中真实发生过的错误，按**错误类型**分组而不是按时间。
每一条都写清：现象、根因、修法、以及**现在靠什么防住它**——只写不防等于没记。

排在最前面的三类各自重复发生过三到四次，是最该警惕的。

---

## 一、失败却报成功（重复 3 次，其中一次造成数据丢失）

**最严重的一类。** 一个环节失败了，但上层看到的是绿灯，于是没人去看。

| 现象 | 根因 | 现在靠什么防住 |
|---|---|---|
| 云端 11 篇中文内容全部丢失 | 推送重试循环用 `git push && break`，循环结束后不检查是否成功，也没有 `exit 1`。日志最后一行是 `push retry 8`，步骤却报 success，runner 销毁后 git 历史里查不到痕迹 | 两个循环都检查 `ok` 变量，失败时 `::error::` + `exit 1`，并在日志里写明"本轮 N 篇会随 runner 销毁" |
| 12 次模型调用全挂，站上不增不减，看起来像"今天没内容" | `run.py` 正常退出 0 | 「每次模型调用都失败且零发布」退出码 5，日志明写"这不是今天没内容，是部署有问题" |
| 评审调用失败时放行，等于评审一坏闸门就不存在 | `review.passes(None)` 返回 True | 改为返回 False：不发布、记软失败、下一轮重试。有回归测试 `test_review_unavailable_does_not_pass` |

**通用规则**：任何"失败时放行"的默认值都要问一句——如果这一层永久坏掉，我多久才会发现？
答案是"不会"就说明方向错了。

例外是有意为之的：选题闸门失灵时**应该**放行（它只是省钱的预筛，放行让稿子走到
成稿评审那道真闸门去），这个区别写进了 `test_triage_unavailable_does_pass`。

| 日更 cron 整轮失败，站上不更新 | `run.py` 发布成功后会自己 build，留下未跟踪的 `p/` 与 `s/`；紧接着 workflow 的 `git pull --rebase` 要覆盖它们，报 `could not detach HEAD` 中止，8 次重试全败 | CI 调用加 `--no-build`（workflow 后面统一从合并后的数据重建），并在 rebase 前 `git clean -fdq p s`。检查见 `test_ci_does_not_build_before_rebasing` |

**这一条和第一条是同源的**：生成产物参与了合并。数据文件名唯一、永不冲突；
生成产物应该从合并后的数据重建，任何让它们进入 merge/rebase 的写法都会周期性
炸掉，而且炸在推送环节——正好是最容易被误报成 success 的地方。

---

## 一之二、让还在调的脚本改共享状态，然后汇报时没核对

新加的信源策展器我一边调一边跑，而它每轮都直接写 `data/sources.json`。两轮旧标准
的候选就这样进了清单：从 61 档变成 71 档，其中包括衍生源「硅谷101|中国版」——
它先进来之后，下一轮去重反而把**原版硅谷101**判成了重复。

更糟的是，我在汇报"新标准判别力更强"的时候，**根本没检查清单被改成了什么样**。
是下一轮的去重日志替我发现的。

修法：`curate.py` 加 `--dry-run`（只出建议，一个字不写）。定时任务保留自动执行，
但**自己调试时一律 dry-run**。已回滚到 61 档。

**通用规则**：一个还在改的判据，不该有权限直接修改会被后续步骤读取的共享状态。
汇报前跑一次 `git status` / `git diff --stat`，看清自己动了什么。

---

## 二、基础设施抖动被当成内容缺失（重复 5 次）

网络抖动、限流、配额用尽，被记成"这一集没有文稿"，消耗完重试预算后**永久拉黑**
一集本来合格的内容。

| 现象 | 根因 |
|---|---|
| 模型 529 Overloaded 让一集永久失败 | HTTP 后端有退避，CLI 后端没有，529 只表现为非零退出码 |
| CLI 多实例并发直接退出 1、无输出 | `--max-turns 1` 太紧，思考用掉那一个 turn 就被判超限 |
| 有字幕的硅谷101 E249 被自己的诊断跑批拉黑 | YouTube 429 限流和 CI 的机器人拦截都被记成 no-transcript |
| `deepseek-reasoner` 12/12 全挂 | 推理 token 计入 `max_tokens`，8000 被思考用光，`content` 返回空字符串，报错只说"没返回 JSON" |
| 策展的"feed 失效 → 移除"会误删好源 | Rational Reminder 的频道 ID 经搜索确认正确，但 Atom feed 返 404（同一天的体检还是 ok/15 条）—— YouTube 对密集请求限流。一次失败就删源，而**误删一个好源没人会注意到** |

第五次之后的修法不再是打补丁：`resolve_sources --check` 记 `fail_streak`，
成功归零、失败累加；策展的移除规则要求连续 3 次失败。
检查见 `test_source_removal_needs_a_persistent_failure`。

**现在的防法**：`transcript.last_was_transient()` 让取稿层告诉编排器失败的**性质**。
限流／要求登录／连接中断／ASR 网络失败算软失败（上限 8，不消耗重试预算）；
只有 yt-dlp 明确报 `has no automatic captions` 这类确定性结论才算硬失败（上限 3）。
推理模型自动抬 `max_tokens`，空 content 时报错直接指出是预算被思考吃光。

---

## 三、校验规则误杀真内容（重复 3 次，都在数字上）

闸门本身是对的，但判据太窄，把真的内容删掉了。**误杀比漏杀更难发现**，因为
日志上看起来是"闸门在工作"。

三次都是同一个盲区：**口语里的数字不是写出来的**。

| 成稿写的 | 逐字稿里实际是 | 第几次踩到 |
|---|---|---|
| 2014 年 | twenty fourteen | 第一次 |
| 约 400 美元 | four hundred dollars | 第一次 |
| 250 人/平方公里 | two hundred **and** fifty | 第二次 |
| 15 亿 | one point five billion | 第二次 |
| 125 亿 | twelve **and a half** billion | 第二次 |
| 2.2 亿 → 220 million | 浮点噪声 `2.2*1e8/1e6 = 220.00000000000003`，精确比较把整数误判成小数 | 第三次 |

**现在的防法**：`gate._renderings()` 把每个数字展开成所有可能的口语形态（英文单词、
年份读法「nineteen ninety one」、`and` 变体、分数惯用语、中文数字、跨单位换算），
并用容差比较浮点。这些真实逐字稿原句全部写进了 `tests/test_units.py::Numbers`。

同类误杀还有一次不在数字上：归属校验的中文分支用贪婪正则把标题切成整个短语
（「关于理想造芯」「这是你该知道的一切」），逐字稿里永远不会出现这种标题式说法，
于是一份**正确的 80 分钟本地转写被丢掉**。改成拉丁词 + 中文 3-gram 双通道。

---

## 四、按过时结论继续推理（重复 2 次）

环境变了，但我还在用旧结论说话，而且是**对用户说**。

- 转写打通之后，我仍然说"硅谷101、科技早知道只有 YouTube 字幕，云端 IP 被拦，
  必须本机跑"。实测六档中文源每集都带音频，ASR 在云端能跑，六档云端全都能做。
- 同一天我又说本机线"只对 5 档必要、优先级低"；用户把重心转到 YouTube 之后，
  这话立刻不成立——纯 YouTube 源只能住宅 IP 抓，本机线变成必需。

**防法只能靠自觉**：每次解开一个约束，回头重算依赖它的所有结论。写在这里是为了
下次改动能力边界时先做这一步。

---

## 五、字符串替换的锚点没匹配，静默失败（重复 2 次）

用脚本改代码时，锚点因为上一轮改动已经变了，`str.replace` 找不到就什么也不做，
**不报错**。

- 加分角色模型时改了 `_cli` 的调用处，签名的替换没匹配上 → `TypeError: _cli() got
  an unexpected keyword argument 'model'`。CI 走 API 路径所以一直正常，只在本机炸，
  10 篇补评全废。
- 首页 JSON-LD 的插入没匹配上，自查时才发现首页没有结构化数据。

**防法**：所有脚本化改动都用 `assert old in s` 断言锚点存在（现在是习惯），
改完立刻跑一次实际调用而不是只 import。回归测试
`test_cli_backend_accepts_a_model` 直接检查签名。

---

## 六、信源假设没验证就当结论

- 以为 Conversations with Tyler 和半拿铁"官网有文稿可走 page 层"，实测拿不到。
  两个都在加源前被挡下了，因为**加源流程强制实测文稿可得性**。
- iTunes 按名字匹配给出 9 个"有 RSS"的 YouTube 源，逐个比对集标题后只有 3 个是
  同一档节目。最危险的是 Anthropic 那条 `feeds.podcastai.com`——一个**冒用品牌的
  AI 生成新闻播客**。按名字匹配就加，站上会出现挂着 Anthropic 名头的机器编内容。

**防法**：加源必须实测取到文稿；换 feed 必须比对集标题与已知渠道，标题对不上一律
不用。`resolve_sources.py --check` 每周跑一次。

---

## 七、其他一次性的

- **SSL 只修了一半**：用 truststore 只给自己的 urllib 建了上下文，没有全局注入，
  于是 `huggingface_hub` 下模型时照样 `CERTIFICATE_VERIFY_FAILED`。修成
  `inject_into_ssl()` + 从 keychain 导出 PEM 并设 `SSL_CERT_FILE` /
  `REQUESTS_CA_BUNDLE` / `HF_HUB_CA_BUNDLE`。
- **把本地时间当成 UTC**，算出跑批"卡死 14 小时"（实际 21 分钟），**取消了一个健康
  的跑批**。现在报时间一律先 `date -u`。
- **直接跑 `run.py` 而不是 `local-daily.sh`**：前者不含提交推送，8 篇产出躺在本地
  没上线（本地 35 / 线上 27），用户问起才发现。
- **复制了被终端截断的 URL**（显示限 52 字符），拿去请求得到 404/403，一度以为是
  源坏了。
- **`.tag` 类名撞车**：masthead 的 slogan 和卡片上的标签小胶囊同名，slogan 因此套上
  了边框和底色。改名 `.slogan`，并写脚本扫全站同类冲突。
- **h1 数量**：单集页原来有两个（报头字标 + 文章标题）；改成字标只在首页当 h1 之后，
  信源页和 404 又变成零个。现在每页恰好一个，自查脚本会数。
- **`git add -A` 把源码扫进 bot 提交**：`local-daily.sh` 的提交把我未提交的
  `run.py` 改动打包进了 "build: regenerate site"。改成只 add 数据与生成产物的具体路径。

---

## 八、判据把「可核对」等同于「有价值」

用户反问「理财哲理漫谈为什么不好，我还挺喜欢哲学」。他是对的。

整个站的前提是"每条判断都能跳回原声核对"，于是我不自觉地把可核对等同于有价值：
数字多的加分，讲道理的减分。一集 Bill Bernstein 讲「哪些理财决定真正重要」被拒，
理由写着"无可核对数据或机制"。

真正的分界线不是有没有数字，是**能不能被反驳**：
  「要长期投资、控制情绪」                                   无法反驳的套话 → 拒
  「只有储蓄率、资产配置、不恐慌三件事重要，其余全是噪音」        有立场能反驳 → 收

triage / review / curate 三处判据都改了：明写价值有两种形态，形态二是判断与框架
（有立场能被反驳的论断、把复杂问题拆成少数起作用的变量、挑战流行共识并给出边界），
思想史与方法论只要得出可争辩的结论就算高分；低分那条改为"泛泛而谈的正确话"。

**一般化的教训**：把工程上的约束（我们能验证什么）写成了产品上的judgment
（什么是好内容）。这两件事必须分开——前者决定"能不能做"，后者决定"该不该做"。
同一个错误在策展评分里也犯过一次：文稿可得性占 3 分，于是一档有官方逐字稿的
平庸节目起步就有分，"能不能做"买到了"该不该做"的通行证。

---

## 九、评分锚定在及格线（重复 2 次）

第一次在成稿评审：已上站的 16 篇分数**全部正好 8**。第二次在信源策展：一批候选
**7 档全部正好 8.0**，而理由栏里写着"补位有限""部分集数偏泛"。

两次都是同一个成因：判据把及格线写成了"合格"、把更高分写成了"罕见"，于是模型把
所有正常候选挤到及格线上。**全部拦稿权力压在 1 分的间隙上，对噪声极度敏感。**

修法两条，缺一不可：
1. 提示词里明说"如果你发现自己给一批候选打的分都一样，那不是在评分，是在往及格线
   上凑"，并把区间每一档写清。
2. **不要指望模型自觉，用代码判一致性**：理由里含"重叠／偏泛／有限／不稳"这类词
   而分数刚够线的，一律不收。实测拦下 4 档自相矛盾的 8.0，留下 Darknet Diaries
   10.0（密度5/补位3/可核对2）。

---

## 十、同一个函数改三次才对

`_name_match`（判断搜到的节目是不是想找的那一档）：
1. 10 字符前缀 → 搜「Patrick」匹配到了别的节目
2. 按最短一方的比例算命中数 → 把 show 当停用词过滤后对方只剩一个 token，
   「Tomorrow Today」匹配上了「The Tomorrow Show」
3. Jaccard ≥ 0.5 → 「This Week in Tech」和「This Week in Startups」判成同一档，
   靠 this/week 撑起相似度
4. Jaccard ≥ 0.5 **且交集里必须有一个有辨识度的词** → 6 个用例全对

`slug_for`（信源 id，出现在 URL 里）：把非 ASCII 全删，于是「42章经」塌成 `42`、
「脑放电波」塌成 `src`、「AI实话实说」塌成 `ai`。改为纯中文名用 `cn` + 名字哈希。

**教训**：字符串相似度这类函数，每改一版都要把**前几版失败的用例一起留在测试里**，
否则修 A 会打破 B。现在这些用例都在 `tests/test_units.py` 与 `test_guards.py` 里。

---

## 评审自身的两个设计缺陷

值得单独记，因为它导致我连续两轮判错方向。

通过率只有 12-21%，14/24 篇卡在 7 分（及格线 8）。我先怀疑生成、改了提示词，
结果更差。真因在评审：

1. **「选择力」这条标准在结构上无法满足**。两万五千字压成 5-8 条要点必然丢掉绝大
   部分，评审只要想挑永远挑得出遗漏，于是成了一项恒定扣分。
2. **更糟：评审在评判它没见过的东西**。给它的证据只有"每条引用时间戳前后 45 秒"，
   全都是成稿已经引用过的区域。它看不到未引用段落，却反复以"漏掉了更值钱的内容"
   扣分。
3. 打分基准把 8 写成"合格"、9-10 写成"值得转给同行"，读起来像 8 才是优秀线，于是
   所有正常稿子被挤进 7-8 两档——**已上站的 16 篇分数全部正好 8**。那不是评分，
   是二元判断，而全部拦稿权力压在 1 分的间隙上，对噪声极度敏感。

修法：明写"遗漏本身不是缺点，说不出具体的 B 就按满分算"；补第二份证据（全篇等距
抽样 4000 字）并声明抽样之外不构成遗漏指控；把区间展开并明说"如果你发现自己给几乎
所有稿子都打 7 或 8，那是你在做二元判断，等于把这套标准废掉了"。

**一般化的教训**：给模型的判据必须是它**能用手上的材料做出的判断**。判据要求它知道
它看不到的东西时，它不会说"我不知道"，而会编一个理由扣分。
