.cache/
__pycache__/
*.pyc
.DS_Store

# 本地预览用的目录：里面是指向仓库自身的符号链接，
# 一旦入库，GitHub Pages 打包会顺着它无限递归，卡死 Upload artifact。
.preview/
.claude/

# repoint.py 的断点记录：一次性回填的本机状态，不该进仓库
data/repoint-done.json

# translate.py 的断点记录：一次性回填的本机状态
data/translate-done.json

# 英文站已入库。零漏译闸门在 preflight 和构建里都拦着，
# 未译的集不渲染（宁可少几篇，也不要中英混排）。
